[package]
name = "plugin"
version = "0.1.0"
edition = "2018"

[lib]
crate-type = ["dylib"]

[dependencies]
async-trait = "0.1.51"
common = { path = "../common" }
tokio = { version = "1", features = ["full"] }
